# what if amtraker ran with nginx lol

REPLACE `runner` IN THE `nginx.conf` WITH THE RUNNING USER

just run start.sh ig

should work on replit and if im not lazy, railway as well. can basically run anywhere with bash and nginx installed.

this is a proof of concept.